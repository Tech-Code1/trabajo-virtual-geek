const totalCuenta = 128
const personas = 9

const cuentaDividida = totalCuenta / personas;

const adicionAna = 2
const totalAna = cuentaDividida + adicionAna;

console.log(`cada persona tiene que pagar ${cuentaDividida}€, 
Ana tiene que pagar un poco mas y su total es de ${totalAna}€`)